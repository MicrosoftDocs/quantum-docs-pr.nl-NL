### YamlMime:QSharpType
uid: microsoft.quantum.diagnostics.assertqubitisinstatewithintolerance
name: AssertQubitIsInStateWithinTolerance
type: operation
namespace: Microsoft.Quantum.Diagnostics
summary: >-
  Beweringen dat een Qubit de verwachte status heeft.


  `expected` vertegenwoordigt een complexe vector, $ \ket{\psi} = \begin{bmatrix}a & b\end {bmatrix} ^ {\mathrm{T}} $.

  Het eerste element van de Tuples die elk van $a $ vertegenwoordigen, $b $ het reële deel van het complexe getal is, terwijl de tweede het meest imaginaire deel uitmaakt.

  Met het laatste argument wordt de tolerantie gedefinieerd waarmee de bevestiging wordt gemaakt.
remarks: >-
  De volgende Mathematica-code kan worden gebruikt om expressies te controleren voor mi, MX, my, MZ:


  ```mathematica

  {Id, X, Y, Z} = Table[PauliMatrix[k], {k, 0, 3}];

  st = {{ reA + I imA }, { reB + I imB} };

  M = st . ConjugateTranspose[st];

  mx = Tr[M.X] // ComplexExpand;

  my = Tr[M.Y] // ComplexExpand;

  mz = Tr[M.Z] // ComplexExpand;

  mi = Tr[M.Id] // ComplexExpand;

  2 m == Id mi + X mx + Z mz + Y my // ComplexExpand // Simplify

  ```


  De tolerantie is de $L\_{\infty} $ distance van de 3D-dimensionale vector (x ₂, x ₃, x ₄) die is gedefinieerd door $ \langle\psi | \psi\rangle = x\_1 I + x\_2 X + x\_3 Y + x\_4 Z $ en Real vector (Y ₂ , y ₃, y ₄) gedefinieerd door ρ = y ₁ I + y ₂ X + y ₃ Y + y ₄ Z waarbij ρ de dichtheids matrix is die overeenkomt met de status van het REGI ster.

  Dit geldt alleen voor de veronderstelling dat tr (ρ) en tr (| ψ ⟩ ⟨ ψ |) beide 1 zijn (bijvoorbeeld x ₁ = 1/2, y ₁ = 1/2).

  Als dit niet het geval is, beweringt de functie dat l ∞ afstand tussen (x ₂-x ₁, x ₃-x ₁, x ₄-x ₁, x ₄ + x ₁) en (y ₂-y ₁, y ₃-y ₁, y ₄-y ₁, y ₄ + y ₁) kleiner is dan de tolerantie parameter.
examples: >-
  ```Q#

  using (qubits = Qubit[2]) {
      // Both qubits are initialized as |0〉: a=(1 + 0*i), b=(0 + 0*i)
      AssertQubitIsInStateWithinTolerance((Complex(1., 0.), Complex(0., 0.)), qubits[0], 1e-5);
      AssertQubitIsInStateWithinTolerance((Complex(1., 0.), Complex(0., 0.)), qubits[1], 1e-5);
      Y(qubits[1]);
      // Y |0〉 = i |1〉: a=(0 + 0*i), b=(0 + 1*i)
      AssertQubitIsInStateWithinTolerance((Complex(0., 0.), Complex(0., 1.)), qubits[1], 1e-5);
  }

  ```
syntax: 'operation AssertQubitIsInStateWithinTolerance (expected : (Microsoft.Quantum.Math.Complex, Microsoft.Quantum.Math.Complex), register : Qubit, tolerance : Double) : Unit'
input:
  content: '(expected : (Microsoft.Quantum.Math.Complex, Microsoft.Quantum.Math.Complex), register : Qubit, tolerance : Double)'
  types:
  - name: expected
    summary: Er werden respectievelijk complexe amplitudes verwacht voor $ \ket{0}$ en $ \ket{1}$.
    types:
    - uid: microsoft.quantum.math.complex
    - uid: microsoft.quantum.math.complex
  - name: register
    summary: Qubit waarvan de status moet worden bevestigd. Houd er rekening mee dat deze Qubit wordt verondersteld te worden gescheiden van andere toegewezen qubits en niet Entangled.
    isPrimitive: true
    uid: Qubit
  - name: tolerance
    summary: >-
      Toevoegings tolerantie waarbij de werkelijke amplitudes mogen afwijken van verwacht.

      Zie de opmerkingen hieronder voor meer informatie.
    isPrimitive: true
    uid: Double
output:
  content: Unit
  types:
  - isPrimitive: true
    uid: Unit
metadata:
  ms.openlocfilehash: 8b43bac19173c5ae5a11ff6879a03209c32e7c76
  ms.sourcegitcommit: edcf15044d7bdf4f8b21fb8f6af4bde475eb13a0
  ms.translationtype: MT
  ms.contentlocale: nl-NL
  ms.lasthandoff: 11/05/2019
  ms.locfileid: "73227114"
