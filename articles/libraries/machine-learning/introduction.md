---
title: Quantum machine learning bibliotheek
author: alexeib2
ms.author: alexei.bocharov@microsoft.com
ms.date: 11/22/2019
ms.topic: article
uid: microsoft.quantum.libraries.machine-learning.introduction
ms.openlocfilehash: 4c42612fee3a58e15368677bb2c77a70c5680f45
ms.sourcegitcommit: 6ccea4a2006a47569c4e2c2cb37001e132f17476
ms.translationtype: MT
ms.contentlocale: nl-NL
ms.lasthandoff: 02/28/2020
ms.locfileid: "77909786"
---
# <a name="introduction-to-quantum-machine-learning"></a><span data-ttu-id="b434e-102">Inleiding tot Quantum Machine Learning</span><span class="sxs-lookup"><span data-stu-id="b434e-102">Introduction to Quantum Machine Learning</span></span>

## <a name="framework-and-goals"></a><span data-ttu-id="b434e-103">Framework en doel stellingen</span><span class="sxs-lookup"><span data-stu-id="b434e-103">Framework and goals</span></span>

<span data-ttu-id="b434e-104">Quantum encoding en verwerking van informatie is een krachtig alternatief voor klassieke machine learning Quantum-classificaties, met name gegevens coderen in Quantum registers die beknopt zijn ten opzichte van het aantal functies, op systematische wijze Quantum gebruiken entanglement als reken kundige resource en gebruik quantum meting voor klasse-demijner.</span><span class="sxs-lookup"><span data-stu-id="b434e-104">Quantum encoding and processing of information is a powerful alternative to classical machine learning Quantum classifiers, in particular, encode data in quantum registers that are concise relative to the number of features, systematically employ quantum entanglement as computational resource and employ quantum measurement for class inference.</span></span>
<span data-ttu-id="b434e-105">Circuit Center Quantum-classificatie is een relatief eenvoudige Quantum oplossing die gegevens codering combineert met een snel entangling/Disentangling Quantum circuit, gevolgd door meting om klassen labels van gegevens voorbeelden af te leiden.</span><span class="sxs-lookup"><span data-stu-id="b434e-105">Circuit centric quantum classifier is a relatively simple quantum solution that combines data encoding with a rapidly entangling/disentangling quantum circuit followed by measurement to infer class labels of data samples.</span></span>
<span data-ttu-id="b434e-106">Het doel is om te zorgen voor klassieke karakte Rise ring en opslag van subject-circuits, evenals hybride quantum/klassieke training van de para meters van de circuits, zelfs voor extreem grote functie ruimten.</span><span class="sxs-lookup"><span data-stu-id="b434e-106">The goal is to ensure classical characterization and storage of subject circuits, as well as hybrid quantum/classical training of the circuit parameters even for extremely large feature spaces.</span></span>

## <a name="classifier-architecture"></a><span data-ttu-id="b434e-107">Classificatie architectuur</span><span class="sxs-lookup"><span data-stu-id="b434e-107">Classifier architecture</span></span>

<span data-ttu-id="b434e-108">Classificatie is een machine learning taak onder Super visie, waarbij het doel heeft om klassen labels te leiden $\{y_1, y_2, \ldots, y_d\}$ van bepaalde gegevens voorbeelden.</span><span class="sxs-lookup"><span data-stu-id="b434e-108">Classification is a supervised machine learning task, where the goal is to infer class labels $\{y_1,y_2,\ldots,y_d\}$ of certain data samples.</span></span> <span data-ttu-id="b434e-109">De ' training data set ' is een verzameling voor beelden van $ \mathcal{D} =\{(x, y)} $ met bekende vooraf toegewezen labels.</span><span class="sxs-lookup"><span data-stu-id="b434e-109">The "training data set" is a collection of samples $\mathcal{D}=\{(x,y)}$ with known pre-assigned labels.</span></span> <span data-ttu-id="b434e-110">Hier $x $ een gegevens voorbeeld is en $y $ is het bekend label met de naam training label.</span><span class="sxs-lookup"><span data-stu-id="b434e-110">Here $x$ is a data sample and $y$ is its known label called "training label".</span></span>
<span data-ttu-id="b434e-111">Wat lijkt op traditionele methoden, de Quantum classificatie bestaat uit drie stappen:</span><span class="sxs-lookup"><span data-stu-id="b434e-111">Somewhat similar to traditional methods, quantum classification consists of three steps:</span></span>
- <span data-ttu-id="b434e-112">gegevens codering</span><span class="sxs-lookup"><span data-stu-id="b434e-112">data encoding</span></span>
- <span data-ttu-id="b434e-113">voor bereiding van een classificatie status</span><span class="sxs-lookup"><span data-stu-id="b434e-113">preparation of a classifier state</span></span>
- <span data-ttu-id="b434e-114">meting als gevolg van de Probabilistic aard van de meting, moeten deze drie stappen meerdere keren worden herhaald.</span><span class="sxs-lookup"><span data-stu-id="b434e-114">measurement Due to the probabilistic nature of the measurement, these three steps must be repeated multiple times.</span></span> <span data-ttu-id="b434e-115">De meting kan worden gezien als een Quantum equivalent van niet-lineaire activering.</span><span class="sxs-lookup"><span data-stu-id="b434e-115">The measurement may be viewed as a quantum equivalent of non-linear activation.</span></span>
<span data-ttu-id="b434e-116">De code ring en het berekenen van de classificatie status worden uitgevoerd door middel van *Quantum circuits*.</span><span class="sxs-lookup"><span data-stu-id="b434e-116">Both the encoding and the computing of the classifier state are done by means of *quantum circuits*.</span></span> <span data-ttu-id="b434e-117">Terwijl het coderings circuit meestal gegevensgestuurd en zonder para meters is, bevat het classificatie circuit een voldoende set leer bare para meters.</span><span class="sxs-lookup"><span data-stu-id="b434e-117">While the encoding circuit is usually data-driven and parameter-free, the classifier circuit contains a sufficient set of learnable parameters.</span></span> 

<span data-ttu-id="b434e-118">In de voorgestelde oplossing is het Classifier-circuit samengesteld uit single-Qubit rotaties en twee Qubit bestuurde rotaties.</span><span class="sxs-lookup"><span data-stu-id="b434e-118">In the proposed solution the classifier circuit is composed of single-qubit rotations and two-qubit controlled rotations.</span></span> <span data-ttu-id="b434e-119">De para meters die hier kunnen worden beschreven, zijn de draai hoek.</span><span class="sxs-lookup"><span data-stu-id="b434e-119">The learnable parameters here are the rotation angles.</span></span> <span data-ttu-id="b434e-120">De rotatie-en beheerde rotatie poorten zijn bekend als *universeel* voor Quantum berekeningen, wat betekent dat elke matrix van unitary Weight kan worden opgesplitst in een lang genoeg circuit dat uit dergelijke Gates bestaat.</span><span class="sxs-lookup"><span data-stu-id="b434e-120">The rotation and controlled rotation gates are known to be *universal* for quantum computation, which means that any unitary weight matrix can be decomposed into a long enough circuit consisting of such gates.</span></span>

![Gecentreerde classificatie met meerdere lagen Perceptron versus circuit](~/media/DLvsQCC.png)

<span data-ttu-id="b434e-122">We kunnen dit model vergelijken met een Perceptron met meerdere lagen om een beter inzicht te krijgen in de basis structuur.</span><span class="sxs-lookup"><span data-stu-id="b434e-122">We can compare this model to a multilayer perceptron to get a better understanding of the basic structure.</span></span> <span data-ttu-id="b434e-123">In de Perceptron wordt de Voorspellings $p (y | x, \theta) $ parametrized door de set gewichten $ \theta $ waarmee de lineaire functies worden bepaald waarmee de niet-lineaire activerings functies worden verbonden (neurons).</span><span class="sxs-lookup"><span data-stu-id="b434e-123">In the perceptron the predictor $p(y|x, \theta)$ is parametrized by the set of weights $\theta$ that determine the linear functions connecting the non-linear activation functions (neurons).</span></span> <span data-ttu-id="b434e-124">Deze para meters kunnen worden getraind om het model te maken.</span><span class="sxs-lookup"><span data-stu-id="b434e-124">These parameters can be trained to create the model.</span></span> <span data-ttu-id="b434e-125">In de uitvoer slaag kan de kans van een voor beeld dat tot een klasse behoort, worden verkregen door gebruik te maken van niet-lineaire activerings functies, zoals Softmax.</span><span class="sxs-lookup"><span data-stu-id="b434e-125">At the output layer we can get the probability of a sample belonging to a class by using non-linear activation functions like softmax.</span></span> <span data-ttu-id="b434e-126">In de classificatie van het circuit Center is de voor spelling parametrized door de draai hoek van de door de Qubit beheerde rotaties van het model circuit.</span><span class="sxs-lookup"><span data-stu-id="b434e-126">In the circuit centric classifier the predictor is parametrized by the rotation angles of the single-qubit and two-qubit controlled rotations of the model circuit.</span></span> <span data-ttu-id="b434e-127">Op een vergelijk bare manier kunnen die para meters worden getraind met een hybride quantum/klassieke versie van het Daal-algoritme voor de kleur overgang.</span><span class="sxs-lookup"><span data-stu-id="b434e-127">In a similar fashion, those parameters can be trained by an hybrid quantum/classical version of the gradient descent algorithm.</span></span> <span data-ttu-id="b434e-128">Voor het berekenen van de uitvoer, in plaats van het gebruik van niet-lineaire activerings functies, wordt de waarschijnlijkheid van de klasse verkregen door herhaalde metingen te lezen over een specifieke Qubit na de beheerde rotaties.</span><span class="sxs-lookup"><span data-stu-id="b434e-128">To calculate the output, instead of using non-linear activation functions, the probability of the class is obtained by reading repeated measurements over a specific qubit after the controlled rotations.</span></span> <span data-ttu-id="b434e-129">Als u de klassieke gegevens in een Quantum status wilt coderen, gebruiken we een instelbaar coderings circuit voor status voorbereiding.</span><span class="sxs-lookup"><span data-stu-id="b434e-129">To encode the classical data in a quantum state we use a controllable encoding circuit for state preparation.</span></span>

<span data-ttu-id="b434e-130">Onze architectuur verkent relatief recente circuits, die daarom snel moeten worden *entangling* om alle correlaties tussen de gegevens functies in alle bereiken vast te leggen.</span><span class="sxs-lookup"><span data-stu-id="b434e-130">Our architecture explores relatively shallow circuits, which therefore must be *rapidly entangling* in order to capture all the correlations between the data features at all ranges.</span></span> <span data-ttu-id="b434e-131">Hieronder ziet u een voor beeld van het handigste snelle entangling-circuit onderdeel.</span><span class="sxs-lookup"><span data-stu-id="b434e-131">An example of the most useful rapidly entangling circuit component is shown on figure below.</span></span> <span data-ttu-id="b434e-132">Hoewel een circuit met deze geometrie uit slechts $3 n + 1 $ Gates bestaat, zorgt de unitary-matrix van het gewicht dat deze reken kundige invloed heeft op een aanzienlijke Kruis communicatie tussen $2 ^ n $-functies.</span><span class="sxs-lookup"><span data-stu-id="b434e-132">Even though a circuit with this geometry consists of only $3 n+1$ gates, the unitary weight matrix that it computes ensures significant cross-talk between $2^n$ features.</span></span>

![Snel entangling Quantum circuit op 5 qubits (met twee cyclische lagen).](~/media/5-qubit-qccc.png)

<span data-ttu-id="b434e-134">Het circuit in het bovenstaande voor beeld bestaat uit 6 single-Qubit Gates (G_1, \ldots, G_5; G_{16}) $ en 10 2-qubits Gates $ (G_6, \ldots, G_{15}) $.</span><span class="sxs-lookup"><span data-stu-id="b434e-134">The circuit in the above example consists of 6 single-qubit gates $(G_1,\ldots,G_5; G_{16})$ and 10 two-qubits gates $(G_6,\ldots,G_{15})$.</span></span> <span data-ttu-id="b434e-135">Ervan uitgaande dat elk van de poorten is gedefinieerd met één para meter die kan worden leren, hebben we 16 para meters die kunnen worden leren, terwijl de dimensie van de 5-Qubit Hilbert-ruimte 32 is.</span><span class="sxs-lookup"><span data-stu-id="b434e-135">Assuming that each of the gates is defined with one learnable parameter we have 16 learnable parameters, while the dimension of the 5-qubit Hilbert space is 32.</span></span> <span data-ttu-id="b434e-136">Een dergelijk circuit geometrie kan eenvoudig worden gegeneraliseerd tot een $n $-Qubit-REGI ster, wanneer $n $ oneven is, circuits met $3 n + 1 $ para meters voor $2 ^ n $-dimensionale functie ruimte.</span><span class="sxs-lookup"><span data-stu-id="b434e-136">Such circuit geometry can be easily generalized to any $n$-qubit register, when $n$ is odd, yielding circuits with $3 n+1$ parameters for $2^n$-dimensional feature space.</span></span>

## <a name="classifier-training-as-a-supervised-learning-task"></a><span data-ttu-id="b434e-137">Geclassificeerde training als een gesuperd leer taak</span><span class="sxs-lookup"><span data-stu-id="b434e-137">Classifier training as a supervised learning task</span></span>

<span data-ttu-id="b434e-138">De training van een classificatie model omvat het vinden van optimale waarden van de operationele para meters, zodat deze de gemiddelde waarschijnlijkheid van het uitstellen van de juiste opleidings labels in de trainings voorbeelden maximaliseren.</span><span class="sxs-lookup"><span data-stu-id="b434e-138">Training of a classifier model involves finding optimal values of its operational parameters, such that they maximize the average likelihood of inferring the correct training labels across the training samples.</span></span>
<span data-ttu-id="b434e-139">Hier hebben we zelf met alleen classificatie op twee niveaus, d.w.z. het geval van $d = $2 en slechts twee klassen met de labels $y _1, y_2 $.</span><span class="sxs-lookup"><span data-stu-id="b434e-139">Here, we concern ourselves with two level classification only, i.e. the case of $d=2$ and only two classes with the labels $y_1,y_2$.</span></span>

> [!NOTE]
> <span data-ttu-id="b434e-140">Een methode om onze methoden te generaliseren voor een wille keurig aantal klassen is het vervangen van qubits met qudits, dat wil zeggen Quantum eenheden met $d $ basis Staten en de tweerichtings meting met $d $-Way meting.</span><span class="sxs-lookup"><span data-stu-id="b434e-140">A principled way of generalizing our methods to arbitrary number of classes is to replace qubits with qudits, i.e. quantum units with $d$ basis states, and the two-way measurement with $d$-way measurement.</span></span>

### <a name="likelihood-as-the-training-goal"></a><span data-ttu-id="b434e-141">Waarschijnlijkheid als het trainings doel</span><span class="sxs-lookup"><span data-stu-id="b434e-141">Likelihood as the training goal</span></span>

<span data-ttu-id="b434e-142">Gezien een lees bare Quantum circuit $U (\theta) $, waarbij $ \theta $ een vector van para meters is, en het identificeren van de uiteindelijke meting door $M $, is de gemiddelde kans op het juiste label degelijkheid $ $ \begin{align} \mathcal{L} (\theta) = \frac{1}{| \mathcal{D} |} \left (\ sum_ {(x, y_1) \in\mathcal{D}} P (M = y_1 | U (\theta) x) + \ sum_ {(x, y_2) \in\mathcal{D}} P (M = y_2 | U (\theta) x) \right) \end{align} $ $ waarbij $P (M = y | z) $ de kans is dat $y $ in de Quantum status $z $ wordt gemeten.</span><span class="sxs-lookup"><span data-stu-id="b434e-142">Given a learnable quantum circuit $U(\theta)$, where $\theta$ is a vector of parameters, and denoting the final measurement by $M$, the average likelihood of the correct label inference is $$ \begin{align} \mathcal{L}(\theta)=\frac{1}{|\mathcal{D}|} \left( \sum_{(x,y_1)\in\mathcal{D}} P(M=y_1|U(\theta) x) + \sum_{(x,y_2)\in\mathcal{D}} P(M=y_2|U(\theta) x)\right) \end{align} $$ where $P(M=y|z)$ is the probability of measuring $y$ in quantum state $z$.</span></span>
<span data-ttu-id="b434e-143">Hier is het goed om te begrijpen dat de waarschijnlijke functie $ \mathcal{L} (\theta) $ soepel is in $ \theta $ en de afgeleide in een $ \ theta_j $ kan worden berekend met behulp van het gebruikte Quantum protocol dat wordt gebruikt voor het berekenen van de waarschijnlijke functie.</span><span class="sxs-lookup"><span data-stu-id="b434e-143">Here, it suffices to understand that the likelihood function $\mathcal{L}(\theta)$ is smooth in $\theta$ and its derivative in any $\theta_j$ can be computed by essentially the same quantum protocol as used for computing the likelihood function itself.</span></span> <span data-ttu-id="b434e-144">Hierdoor kunt u de $ \mathcal{L} (\theta) $ per verloop Daal optimaliseren.</span><span class="sxs-lookup"><span data-stu-id="b434e-144">This allows for optimizing the $\mathcal{L}(\theta)$ by gradient descent.</span></span>

### <a name="classifier-bias-and-training-score"></a><span data-ttu-id="b434e-145">Classificatie afwijking en trainings Score</span><span class="sxs-lookup"><span data-stu-id="b434e-145">Classifier bias and training score</span></span>

<span data-ttu-id="b434e-146">Gezien sommige tussenliggende (of laatste) waarden van de para meters in $ \theta $, moeten we een enkele reële waarde identificeren $b $ weet als *Classifier-afwijking* om de deinterferentie uit te voeren.</span><span class="sxs-lookup"><span data-stu-id="b434e-146">Given some intermediate (or final) values of the parameters in $\theta$, we need to identify a single real value $b$ know as *classifier bias* to do the inference.</span></span> <span data-ttu-id="b434e-147">De regel voor het afwijzen van labels werkt als volgt:</span><span class="sxs-lookup"><span data-stu-id="b434e-147">The label inference rule works as follows:</span></span> 
- <span data-ttu-id="b434e-148">Een voor beeld $x $ is toegewezen label $y _2 $ if en alleen als $P (M = y_2 | U (\theta) x) + b > $0,5 (FIREWALLREGEL1) (anders is het label toegewezen $y _1 $)</span><span class="sxs-lookup"><span data-stu-id="b434e-148">A sample $x$ is assigned label $y_2$ if and only if $P(M=y_2|U(\theta) x) + b > 0.5$  (RULE1) (otherwise it is assigned label $y_1$)</span></span>

<span data-ttu-id="b434e-149">Een duidelijk $b $ moet in het interval $ (-0,5, + 0,5) $ liggen om betekenisvol te zijn.</span><span class="sxs-lookup"><span data-stu-id="b434e-149">Clearly $b$ must be in the interval $(-0.5,+0.5)$ to be meaningful.</span></span>

<span data-ttu-id="b434e-150">Een trainings case $ (x, y) \in \mathcal{D} $ wordt beschouwd als een onechte *classificatie* , gezien de bias $b $ als het label dat is uitgesteld voor $x $ zoals per firewallregel1 een andere naam heeft dan $y $.</span><span class="sxs-lookup"><span data-stu-id="b434e-150">A training case $(x,y) \in \mathcal{D}$ is considered a *misclassification* given the bias $b$ if the label inferred for $x$ as per RULE1 is actually different from $y$.</span></span> <span data-ttu-id="b434e-151">Het totale aantal misclassificaties is de *trainings Score* van de classificatie, gezien de bias $b $.</span><span class="sxs-lookup"><span data-stu-id="b434e-151">The overall number of misclassifications is the *training score* of the classifier given the bias $b$.</span></span> <span data-ttu-id="b434e-152">De *optimale* Classifier-afwijking $b $ minimaliseert de trainings Score.</span><span class="sxs-lookup"><span data-stu-id="b434e-152">The *optimal* classifier bias $b$ minimizes the training score.</span></span> <span data-ttu-id="b434e-153">Het is eenvoudig te zien dat, gezien de vooraf berekende waarschijnlijkheid $\{ P (M = y_2 | U (\theta) x) | (x, \*) \in\mathcal{D} \}$, de optimale classificatie afwijking kan worden gevonden door binaire zoek opdracht in interval $ (-0,5, + 0,5) $ door Maxi maal $ \ log_2 te maken (| \mathcal{D} |) $ stappen.</span><span class="sxs-lookup"><span data-stu-id="b434e-153">It is easy to see that, given the precomputed probability estimates $\{ P(M=y_2|U(\theta) x) | (x,\*)\in\mathcal{D} \}$, the optimal classifier bias can be found by binary search in interval $(-0.5,+0.5)$ by making at most $\log_2(|\mathcal{D}|)$ steps.</span></span>

### <a name="reference"></a><span data-ttu-id="b434e-154">Naslaginformatie</span><span class="sxs-lookup"><span data-stu-id="b434e-154">Reference</span></span>

<span data-ttu-id="b434e-155">Deze informatie moet voldoende zijn om met de code te kunnen spelen.</span><span class="sxs-lookup"><span data-stu-id="b434e-155">This information should be enough to start playing with the code.</span></span> <span data-ttu-id="b434e-156">Als u echter meer wilt weten over dit model, leest u het oorspronkelijke voor stel: [ *' circuit-georiënteerde Quantum classificaties ', Maria schuld, Alex Bocharov, Krysta Svore en Nathan Wiebe*](https://arxiv.org/abs/1804.00633)</span><span class="sxs-lookup"><span data-stu-id="b434e-156">However, if you want to learn more about this model, please read the original proposal: [*'Circuit-centric quantum classifiers', Maria Schuld, Alex Bocharov, Krysta Svore and Nathan Wiebe*](https://arxiv.org/abs/1804.00633)</span></span>
