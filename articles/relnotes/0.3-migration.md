---
title: QDK 0,3 taal controle en migratie handleiding
description: "Hierin worden de nieuwe functies in de Microsoft Quantum Development Kit 0,3 beschreven en wordt uitgelegd hoe u bestaande Q #-Program ma's kunt migreren."
author: beheim
uid: microsoft.quantum.relnotes.migration-0-3
ms.author: bettina.heim@microsoft.com
ms.date: 10/29/2018
ms.topic: article
ms.openlocfilehash: df86bf163a300c1ce55d3959bd40aea3d8b8a973
ms.sourcegitcommit: 6ccea4a2006a47569c4e2c2cb37001e132f17476
ms.translationtype: MT
ms.contentlocale: nl-NL
ms.lasthandoff: 02/28/2020
ms.locfileid: "77907729"
---
# <a name="qdk-03-language-release-notes-and-migration-guide"></a><span data-ttu-id="a456d-103">Release opmerkingen en migratie handleiding voor QDK 0,3-taal</span><span class="sxs-lookup"><span data-stu-id="a456d-103">QDK 0.3 language release notes and migration guide</span></span>

<span data-ttu-id="a456d-104">We zijn enthousiast om de nieuwste update uit te voeren op de Quantum Development Kit.</span><span class="sxs-lookup"><span data-stu-id="a456d-104">We are excited to introduce the newest update to the Quantum Development Kit.</span></span>  <span data-ttu-id="a456d-105">Op deze pagina vindt u een overzicht van de nieuwe Q #-taal functies en wordt beschreven hoe u bestaande Q #-Program ma's [migreert](#Migration) naar de nieuwe syntaxis.</span><span class="sxs-lookup"><span data-stu-id="a456d-105">This page gives an overview of the new Q# language features and describes how to [migrate](#Migration) existing Q# programs to the new syntax.</span></span>  <span data-ttu-id="a456d-106">Zie onze volledige documentatie voor de volledige beschrijving van de Q #-taal [hier](xref:microsoft.quantum.language.intro).</span><span class="sxs-lookup"><span data-stu-id="a456d-106">See our complete documentation for the full description of the Q# language [here](xref:microsoft.quantum.language.intro).</span></span>


## <a name="whats-new"></a><span data-ttu-id="a456d-107">Nieuwe functies</span><span class="sxs-lookup"><span data-stu-id="a456d-107">What's New</span></span> 

<span data-ttu-id="a456d-108">De 0,3-update wordt geleverd met een aantal nieuwe taal-en Editor functies.</span><span class="sxs-lookup"><span data-stu-id="a456d-108">The 0.3 update comes with a range of new language and editor features.</span></span>

### <a name="overview-of-features"></a><span data-ttu-id="a456d-109">Overzicht van functies</span><span class="sxs-lookup"><span data-stu-id="a456d-109">Overview of Features</span></span>

- <span data-ttu-id="a456d-110">Verbeterde ontwikkel hulpprogramma's integratie voor Visual Studio en Visual Studio code, waaronder:</span><span class="sxs-lookup"><span data-stu-id="a456d-110">Enhanced development tool integration for Visual Studio and Visual Studio Code, including:</span></span>
    - <span data-ttu-id="a456d-111">Live compilatie en feedback over typen (*ook*</span><span class="sxs-lookup"><span data-stu-id="a456d-111">Live compilation and feedback on typing (*a.k.a.*</span></span> <span data-ttu-id="a456d-112">onderstreping van Kronkel lijn).</span><span class="sxs-lookup"><span data-stu-id="a456d-112">squiggly underlining).</span></span>
    - <span data-ttu-id="a456d-113">Houd informatie, inclusief documentatie overzichten en type hand tekeningen.</span><span class="sxs-lookup"><span data-stu-id="a456d-113">Hover information including documentation summaries and type signatures.</span></span>
    - <span data-ttu-id="a456d-114">Ondersteuning voor Ga naar definitie.</span><span class="sxs-lookup"><span data-stu-id="a456d-114">Support for go to definition.</span></span>
    - <span data-ttu-id="a456d-115">Eenvoudige navigatie naar declaraties van naam ruimte, bewerking, functie en type.</span><span class="sxs-lookup"><span data-stu-id="a456d-115">Easy navigation to namespace, operation, function, and type declarations.</span></span>
    - <span data-ttu-id="a456d-116">Verbeterde diagnostische berichten.</span><span class="sxs-lookup"><span data-stu-id="a456d-116">Improved diagnostic messages.</span></span>
- <span data-ttu-id="a456d-117">Taal verbeteringen</span><span class="sxs-lookup"><span data-stu-id="a456d-117">Language improvements</span></span>
    - <span data-ttu-id="a456d-118">Enkele Qubit-en tuple-initialisaties binnen het gebruik en het lenen van</span><span class="sxs-lookup"><span data-stu-id="a456d-118">Single qubit and tuple initializations within using and borrowing</span></span>
    - <span data-ttu-id="a456d-119">Tuple-ontbouwing voor alle toewijzingen</span><span class="sxs-lookup"><span data-stu-id="a456d-119">Tuple deconstruction on all assignments</span></span>
    - <span data-ttu-id="a456d-120">Herhaling over matrices</span><span class="sxs-lookup"><span data-stu-id="a456d-120">Iteration over arrays</span></span>
    - <span data-ttu-id="a456d-121">Voorwaardelijke expressies</span><span class="sxs-lookup"><span data-stu-id="a456d-121">Conditional expressions</span></span>
    - <span data-ttu-id="a456d-122">Standaard-specialisatie afkorting voor bewerkingen</span><span class="sxs-lookup"><span data-stu-id="a456d-122">Default-specialization abbreviation for operations</span></span>
    - <span data-ttu-id="a456d-123">Automatisch gegenereerde type constructies</span><span class="sxs-lookup"><span data-stu-id="a456d-123">Auto-generated type constructors</span></span>
    - <span data-ttu-id="a456d-124">Expressie parameters</span><span class="sxs-lookup"><span data-stu-id="a456d-124">Expression modifiers</span></span>


## <a name="editor-features"></a><span data-ttu-id="a456d-125">Editor functies</span><span class="sxs-lookup"><span data-stu-id="a456d-125">Editor Features</span></span>

<span data-ttu-id="a456d-126">De Quantum Development Kit bevat nu een taal server voor Q #, evenals de client integraties voor Visual Studio en Visual Studio code.</span><span class="sxs-lookup"><span data-stu-id="a456d-126">The Quantum Development Kit now includes a language server for Q#, as well as the client integrations for Visual Studio and Visual Studio Code.</span></span>
<span data-ttu-id="a456d-127">Hierdoor zijn meer IntelliSense-functies mogelijk, evenals live feedback bij typen, in de vorm van krabbeltjes onder fouten en waarschuwingen.</span><span class="sxs-lookup"><span data-stu-id="a456d-127">This enables a new set of IntelliSense features along with live feedback on typing in form of squiggly underlinings of errors and warnings.</span></span>
<span data-ttu-id="a456d-128">In het bijzonder gaat u naar definitie wordt nu ondersteund en wordt de aanwijs informatie weer gegeven die type-informatie bevat, evenals informatie over het documenteren van opmerkingen.</span><span class="sxs-lookup"><span data-stu-id="a456d-128">In particular, go to definition is now supported and hover information is displayed containing type information as well as information from documenting comments.</span></span> <span data-ttu-id="a456d-129">Daarnaast is symbool informatie over gedefinieerde naam ruimten, bewerkingen, functies en typen beschikbaar en kunt u snel naar declaraties navigeren.</span><span class="sxs-lookup"><span data-stu-id="a456d-129">Additionally, symbol information about defined namespaces, operations, functions, and types is available and allows to quickly navigate to declarations.</span></span>    
<span data-ttu-id="a456d-130">Met deze update worden diagnostische berichten in het algemeen aanzienlijk verbeterd, met eenvoudige navigatie naar en precieze bereiken voor diagnostische gegevens en aanvullende informatie in de weergegeven, zwevende informatie.</span><span class="sxs-lookup"><span data-stu-id="a456d-130">This update greatly improves diagnostic messages in general, with easy navigation to and precise ranges for diagnostics and additional details in the displayed hover information.</span></span> 

<span data-ttu-id="a456d-131">Voor deze 0,3-versie biedt de taal server die deel uitmaakt van de Quantum Development Kit geen ondersteuning voor meerdere werk ruimten.</span><span class="sxs-lookup"><span data-stu-id="a456d-131">For this 0.3 release, the language server included with the Quantum Development Kit does not support multiple workspaces.</span></span>
<span data-ttu-id="a456d-132">Als u wilt werken met een project in VS code, opent u de hoofdmap met het project zelf en alle projecten waarnaar wordt verwezen.</span><span class="sxs-lookup"><span data-stu-id="a456d-132">In order to work with a project in VS Code, open the root folder containing the project itself and all referenced projects.</span></span>
<span data-ttu-id="a456d-133">Als u wilt werken met een oplossing in Visual Studio, moeten alle projecten in de oplossing zich in dezelfde map bevinden als de oplossing of in een van de submappen.</span><span class="sxs-lookup"><span data-stu-id="a456d-133">In order to work with a solution in Visual Studio, all projects contained in the solution need to be in the same folder as the solution or in one of its subfolders.</span></span>

## <a name="language-features"></a><span data-ttu-id="a456d-134">Taal functies</span><span class="sxs-lookup"><span data-stu-id="a456d-134">Language Features</span></span>

<span data-ttu-id="a456d-135">In termen van taal functies bundelt deze update de behandeling van een bereik van taal patronen.</span><span class="sxs-lookup"><span data-stu-id="a456d-135">In terms of language features, this update unifies the treatment of a range of language patterns.</span></span>
<span data-ttu-id="a456d-136">Type constructors, als voor beeld, worden gegenereerd voor elk door de gebruiker gedefinieerd type en kunnen gedeeltelijk worden toegepast op dezelfde manier als andere functies.</span><span class="sxs-lookup"><span data-stu-id="a456d-136">Type constructors, as an example, are generated for each user defined type and can be partially applied much like any other function.</span></span>
<span data-ttu-id="a456d-137">Een ander voor beeld is het ontbouwen van een tupel, dat nu volledig wordt ondersteund in alle toewijzingen.</span><span class="sxs-lookup"><span data-stu-id="a456d-137">Another example is tuple deconstruction, which is now fully supported within all assignments.</span></span> <span data-ttu-id="a456d-138">Dit omvat niet alleen instructies voor toestaan, onveranderbaar en instellen, maar ook de herhalings variabele in for-lussen, en het gebruik van-en lenende toewijzingen.</span><span class="sxs-lookup"><span data-stu-id="a456d-138">This includes not only let-, mutable-, and set-statements, but also the iteration variable in for-loops as well as using- and borrowing-allocations.</span></span> <span data-ttu-id="a456d-139">Daarnaast worden gedeeltelijke ontbouwingen nieuw ondersteund in de 0,3-update; onderscores in ontbouwingen duiden op delen van de waarde die moeten worden genegeerd.</span><span class="sxs-lookup"><span data-stu-id="a456d-139">Additionally, partial deconstructions are newly supported with the 0.3 update; underscores in deconstructions indicate parts of the value that are to be ignored.</span></span> 

<span data-ttu-id="a456d-140">In de volgende code ziet u enkele van de nieuwe functies:</span><span class="sxs-lookup"><span data-stu-id="a456d-140">The following code illustrates some of the new features:</span></span>
```qsharp
    let tuples = [(1, 0), (0, 1)];
    mutable res = (0, 0, 0);

    // For-loops can iterate over arrays, and can destructure tuples.
    for ((i1, i2) in tuples) {

        // Mutable assignments can now destructure and ignore parts of tuples,
        // using the same syntax as let-bindings.
        mutable (r1, r2, _) = res;
        set (r1, r2) = (r1 ||| i1, r2 &&& i2);

        let (_, _, s) = res;
        // The new conditional operator can be used inside expressions, avoiding
        // the need for extraneous if-statements.
        set res = (r1, r2, r1 == r2 ? s | s + 1);
    }
```
<span data-ttu-id="a456d-141">De laatste set-instructie gebruikt een nieuwe ternaire operator die is geïntroduceerd in de 0,3-update om voorwaardelijke expressies te ondersteunen.</span><span class="sxs-lookup"><span data-stu-id="a456d-141">The last set statement uses a new ternary operator that is introduced with the 0.3 update to support conditional expressions.</span></span>
<span data-ttu-id="a456d-142">Een voorwaardelijke expressie is een expressie van de `condition ? caseTrue | caseFalse`van het formulier.</span><span class="sxs-lookup"><span data-stu-id="a456d-142">A conditional expression is an expression of the form `condition ? caseTrue | caseFalse`.</span></span>
<span data-ttu-id="a456d-143">Zoals in het bovenstaande voor beeld kan worden weer gegeven, wordt iteratie over matrices nu ook ondersteund.</span><span class="sxs-lookup"><span data-stu-id="a456d-143">As can be seen from the example above, iteration over arrays is now also supported.</span></span>

<span data-ttu-id="a456d-144">Toewijzingen binnen `using` en `borrowing` zijn pas mogelijk voor enkelvoudige qubitss, Qubit-matrices en geneste Tuples daarvan:</span><span class="sxs-lookup"><span data-stu-id="a456d-144">Allocations within `using` and `borrowing` are newly possible for single qubits, qubit arrays, and nested tuples thereof:</span></span>
```qsharp
    using (qubit = Qubit()) {
        // qubit contains a single qubit
    }

    borrowing ((qubits, qubit) = (Qubit[3], Qubit())) {
        // qubits contains an array of three qubits, and qubit contains a single qubit
    }
```

<span data-ttu-id="a456d-145">Er zijn twee nieuwe instructies voor het maken van een specialisatie Generator `invert` en `distribute` naast `auto` worden geïntroduceerd om de controle te verg Roten over hoe functor-specialisaties worden gegenereerd.</span><span class="sxs-lookup"><span data-stu-id="a456d-145">Two new specialization generator directives `invert` and `distribute` in addition to `auto` are introduced to increase control over how functor specializations are generated.</span></span>
<span data-ttu-id="a456d-146">Een expliciete declaratie van de standaard `body` specialize in bewerkingen is niet meer verplicht.</span><span class="sxs-lookup"><span data-stu-id="a456d-146">An explicit declaration of the default `body` specialization within operations is no longer mandatory.</span></span>
<span data-ttu-id="a456d-147">Net als bij functies kunnen instructies direct worden toegevoegd aan de bewerkings declaratie zelf als er geen andere specialisatie is gedeclareerd in de bewerking.</span><span class="sxs-lookup"><span data-stu-id="a456d-147">Similar to functions, statements may be directly added to the operation declaration itself if no other specialization is declared within the operation.</span></span>
<span data-ttu-id="a456d-148">Er is een migratie script opgenomen in de release om bestaande code te migreren en te profiteren van nieuwe taal functies (Zie de [sectie over het migratie script](#MigrationScript)).</span><span class="sxs-lookup"><span data-stu-id="a456d-148">A migration script is included in the release to help migrating existing code and taking advantage of new language features (see the [section on the migration script](#MigrationScript)).</span></span>

<span data-ttu-id="a456d-149">Een belang rijke wijziging in de manier waarop door de gebruiker gedefinieerde typen worden behandeld, wordt geleverd met de 0,3-update.</span><span class="sxs-lookup"><span data-stu-id="a456d-149">A major change in how user-defined types are treated comes with the 0.3 update.</span></span> <span data-ttu-id="a456d-150">Hoewel vooraf door de gebruiker gedefinieerde typen worden behandeld als subtypen van het onderliggende type, is dit niet langer het geval (Zie ook de [sectie over het verbreken van wijzigingen](#BreakingChanges)).</span><span class="sxs-lookup"><span data-stu-id="a456d-150">While prior releases treated user defined types as subtypes of their underlying type, this is no longer the case going forward (see also the [section on breaking changes](#BreakingChanges)).</span></span>  
<span data-ttu-id="a456d-151">Als onderdeel van deze wijziging introduceren we expressie wijzigingen die kunnen worden toegepast op bepaalde expressies.</span><span class="sxs-lookup"><span data-stu-id="a456d-151">As part of this change we introduce expression modifiers that can be applied to certain expressions.</span></span> <span data-ttu-id="a456d-152">De ' Atomic ' expressies waarmee para meters kunnen worden toegepast, zijn id's, expressies voor matrix items en ariteit-1 Tuples.</span><span class="sxs-lookup"><span data-stu-id="a456d-152">The "atomic" expressions that modifiers can be applied to are identifiers, array item expressions, and arity-1 tuples.</span></span>
<span data-ttu-id="a456d-153">Aanpassings functies binden een nauwerer dan andere expressie Combinator, en met name dichterer dan de aanroep-Expression Combinator `(` `)`.</span><span class="sxs-lookup"><span data-stu-id="a456d-153">Modifiers bind tighter than any other expression combinator, and in particular tighter than the call-expression combinator `(` `)`.</span></span> 
<span data-ttu-id="a456d-154">`Adjoint`, `Controlled`en een nieuwe achtervoegsel-operator voor het uitpakken `!` worden behandeld als expressie parameters.</span><span class="sxs-lookup"><span data-stu-id="a456d-154">`Adjoint`, `Controlled`, and a new "unwrap" postfix operator `!` are treated as expression modifiers.</span></span> <span data-ttu-id="a456d-155">Als u `Adjoint` en `Controlled` als expressie wijzigt, wordt de nood zaak voor een groot aantal cases overbodig.</span><span class="sxs-lookup"><span data-stu-id="a456d-155">Treating `Adjoint` and `Controlled` as expression modifiers eliminates the need for parenthesis for a lot of cases.</span></span> <span data-ttu-id="a456d-156">De onderstaande expressies zijn bijvoorbeeld geldig voor `Op`, een unitary-bewerking, `opArr` een matrix met unitary-bewerkingen en een geschikt argument `arg`:</span><span class="sxs-lookup"><span data-stu-id="a456d-156">The expressions below for example are valid going forward for `Op`, a unitary operation, `opArr` an array of unitary operations, and `arg` a suitable argument:</span></span> 
```qsharp
    Adjoint Op (arg);
    Controlled opArr[i] (arg);
```
<span data-ttu-id="a456d-157">Het migratie script verwijdert het meren deel van het overbodige haakje en profiteert optimaal van de nieuwe functies.</span><span class="sxs-lookup"><span data-stu-id="a456d-157">The migration script will drop most of the unnecessary parenthesis and take full advantage of the new features.</span></span> <span data-ttu-id="a456d-158">U wordt echter aangeraden om de code niet te interpreteren in het script en maakt geen rekening met de verschillende interpretatie van de `Controlled` functor in zeldzame gevallen (alleen relevant in combi natie met gedeeltelijke toepassingen).</span><span class="sxs-lookup"><span data-stu-id="a456d-158">However, be advised that the script does not interpret the code, and will not account for the different interpretation of the `Controlled` functor in rare cases (only relevant in combination with partial applications).</span></span>

<span data-ttu-id="a456d-159">Met de nieuwe aanpassings functie `!` wordt een door de gebruiker gedefinieerd type geconverteerd naar het onderliggende type.</span><span class="sxs-lookup"><span data-stu-id="a456d-159">The new modifier `!` casts a user-defined type to its underlying type.</span></span> <span data-ttu-id="a456d-160">Deze cast is als een upcast in eerdere releases beschouwd en is automatisch uitgevoerd.</span><span class="sxs-lookup"><span data-stu-id="a456d-160">This cast has been considered an upcast in previous releases and was executed automatically.</span></span> <span data-ttu-id="a456d-161">Met de nieuwe interpretatie van door de gebruiker gedefinieerde typen is dit niet langer het geval en moet de cast expliciet worden gemaakt.</span><span class="sxs-lookup"><span data-stu-id="a456d-161">With the new interpretation of user defined types this is no longer the case and the cast needs to be made explicit.</span></span>    
<span data-ttu-id="a456d-162">In het volgende voor beeld ziet u hoe u de nieuwe aanpassings functie kunt gebruiken:</span><span class="sxs-lookup"><span data-stu-id="a456d-162">The following example illustrates how to use the new modifier:</span></span> 
```qsharp
    newtype Unitary = (Qubit => Unit: Adjoint, Controlled);

    operation Foo (unitaries : Unitary[], qubit : Qubit) : Unit {

        for (unitary in unitaries[1 .. Length(unitaries)-1]) {
            // Each element of unitaries is an instance of the user-defined
            // type Unitary, so unitary! unwraps each element to an operation
            // type that we can call.
            unitary! (qubit);
            // The unwrap operator can also be used as a part of functor
            // expressions and when indexing into arrays.
            Adjoint unitary! (qubit);
            Adjoint unitaries[0]! (qubit);
        }
    }
```
<span data-ttu-id="a456d-163">Zoals in het voor beeld kan worden weer gegeven, bindt `!` dichter bij de opties voor voor voegsels, zoals `Controlled` en `Adjoint`.</span><span class="sxs-lookup"><span data-stu-id="a456d-163">As can be seen from the example, `!` binds tighter than prefix modifiers, like `Controlled` and `Adjoint`.</span></span>

## <a name="BreakingChanges"></a><span data-ttu-id="a456d-164">Wijzigingen afbreken</span><span class="sxs-lookup"><span data-stu-id="a456d-164">Breaking Changes</span></span> 

<span data-ttu-id="a456d-165">De 0,3-update wordt geleverd met een nieuwe compiler en bevat een aantal wijzigingen.</span><span class="sxs-lookup"><span data-stu-id="a456d-165">The 0.3 update comes with a new compiler and contains a handful of breaking changes.</span></span>
<span data-ttu-id="a456d-166">Om het bijwerken van bestaande code mogelijk te maken, biedt de compiler ook een opmaak optie waarmee geldige of ongeldige code wordt gecompileerd en de opgemaakte Q #-code op basis van de ingebouwde compilatie wordt verzonden.</span><span class="sxs-lookup"><span data-stu-id="a456d-166">To facilitate updating existing code, the compiler also provides a formatting option that compiles valid or invalid code and emits formatted Q# code based on the built compilation.</span></span>
<span data-ttu-id="a456d-167">Alle wijzigingen in de syntaxis worden automatisch verwerkt door het meegeleverde hulp programma.</span><span class="sxs-lookup"><span data-stu-id="a456d-167">All syntax changes will be processed automatically by the provided tool.</span></span> <span data-ttu-id="a456d-168">Wijzigingen in de manier waarop geldige code wordt geïnterpreteerd (semantische wijzigingen), moeten echter mogelijk hand matig worden gemaakt.</span><span class="sxs-lookup"><span data-stu-id="a456d-168">However, changes in how valid code is interpreted (semantic changes) may need to be made manually.</span></span> 

<span data-ttu-id="a456d-169">Naast de onderstaande wijzigingen wordt het patroon `__*__` waarbij \* een wille keurige volg orde van niet-spatie tekens is gereserveerd voor intern gebruik en kan niet worden gebruikt als symbool naam.</span><span class="sxs-lookup"><span data-stu-id="a456d-169">In addition to the changes listed below, the pattern `__*__` where \* is any sequence of non-whitespace characters is reserved for internal use and cannot be used as a symbol name.</span></span>  

## <a name="syntax-changes"></a><span data-ttu-id="a456d-170">Syntaxis wijzigingen</span><span class="sxs-lookup"><span data-stu-id="a456d-170">Syntax Changes</span></span> 

- <span data-ttu-id="a456d-171">Met ingang van versie 0,3 worden matrix items niet langer gescheiden door punt komma's, maar worden ze gescheiden door komma's.</span><span class="sxs-lookup"><span data-stu-id="a456d-171">Starting with the version 0.3, array items are no longer separated by semicolons, but are separated by commas instead.</span></span>
- <span data-ttu-id="a456d-172">Er is een punt komma aan de andere kant vereist na een instructie voor het automatisch genereren van de specialisatie declaraties.</span><span class="sxs-lookup"><span data-stu-id="a456d-172">A semicolon on the other hand is required after an auto-generation directive for specialization declarations.</span></span>
- <span data-ttu-id="a456d-173">Voor waarden in if-, Elif-en until-componenten moeten tussen haakjes worden ingekapseld.</span><span class="sxs-lookup"><span data-stu-id="a456d-173">Conditions in if-, elif-, and until-clauses need to be encapsulated in parenthesis.</span></span> 

<span data-ttu-id="a456d-174">Voor alle andere syntaxis wijzigingen wordt de compiler gewoon een waarschuwing gegenereerd als de oude syntaxis wordt gebruikt.</span><span class="sxs-lookup"><span data-stu-id="a456d-174">For all other syntax changes the compiler will simply generate a warning if the old syntax is used.</span></span>     
<span data-ttu-id="a456d-175">Een voor beeld voor een dergelijke wijziging is de symbool-tuple in door de gebruiker gedefinieerde specials.</span><span class="sxs-lookup"><span data-stu-id="a456d-175">One example for such a change is the symbol tuple in user-defined specializations.</span></span> <span data-ttu-id="a456d-176">In eerdere releases heeft een beheerde of Controlled-adjoint-declaratie voor specialisatie een enkel symbool argument opgegeven, beginnend bij 0,3 het wordt aanbevolen om een dergelijke symbool tuple te bieden voor alle specialisatie met `...` die aangeeft dat de compiler de symbolen moet migreren van de aanroep bare declaratie, indien nodig.</span><span class="sxs-lookup"><span data-stu-id="a456d-176">While in previous releases a controlled or controlled-adjoint specialization declaration specified a single symbol argument, starting in 0.3 it is encouraged to provide such a symbol tuple for all specialization with `...` indicating that the compiler should migrate the symbols from the callable declaration if needed.</span></span> 

## <a name="semantic-changes"></a><span data-ttu-id="a456d-177">Semantische wijzigingen</span><span class="sxs-lookup"><span data-stu-id="a456d-177">Semantic Changes</span></span> 

<span data-ttu-id="a456d-178">Het meest impact op de 0,3-release is hoe door de gebruiker gedefinieerde typen worden geïntegreerd in het type systeem.</span><span class="sxs-lookup"><span data-stu-id="a456d-178">The most impactful breaking for the 0.3 release is how user-defined types are integrated into the type system.</span></span> <span data-ttu-id="a456d-179">In eerdere releases werden door de gebruiker gedefinieerde typen beschouwd als subtype van het onderliggende type.</span><span class="sxs-lookup"><span data-stu-id="a456d-179">In prior releases user defined types were considered to be a subtype of their underlying type.</span></span> <span data-ttu-id="a456d-180">Deze versie is niet langer het geval.</span><span class="sxs-lookup"><span data-stu-id="a456d-180">With this release and going forward this is no longer the case.</span></span> <span data-ttu-id="a456d-181">Door de gebruiker gedefinieerde typen worden nu als eigen afzonderlijk type beschouwd en er bestaat geen automatische conversie tussen een door de gebruiker gedefinieerd type en het onderliggende type.</span><span class="sxs-lookup"><span data-stu-id="a456d-181">User defined types are now considered to be their own distinct type, and no automatic cast between a user defined type and its underlying type exists.</span></span> <span data-ttu-id="a456d-182">Alle casts moeten expliciet worden gemaakt met behulp van de operator ' teruglopend ' `!`en er is met name een casting nodig om toegang te krijgen tot de inhoud van een object van een door de gebruiker gedefinieerd type.</span><span class="sxs-lookup"><span data-stu-id="a456d-182">All casts need to be made explicit via an "unwrap" operator `!`, and a cast is needed in particular to access the content of a object of user defined type.</span></span>  

<span data-ttu-id="a456d-183">De wijzigingen aan het type systeem omvatten ook het effect van de variantie van matrices en de behandeling van object typen met para meters.</span><span class="sxs-lookup"><span data-stu-id="a456d-183">The modifications to the type system also include the variance behavior of arrays, and the treatment of type parameterized objects.</span></span> <span data-ttu-id="a456d-184">Vanaf 0,3 zijn zowel onveranderbare als onveranderbare matrices niet-variabel.</span><span class="sxs-lookup"><span data-stu-id="a456d-184">Starting in 0.3, both mutable and immutable arrays are invariant.</span></span> <span data-ttu-id="a456d-185">Daarnaast kunnen argumenten die zijn door gegeven aan een aanroepable, niet van het type para meter zijn.</span><span class="sxs-lookup"><span data-stu-id="a456d-185">Additionally, arguments passed to a callable cannot be type parameterized.</span></span> <span data-ttu-id="a456d-186">In dergelijke gevallen moeten argumenten van het type worden door gegeven samen met de id.</span><span class="sxs-lookup"><span data-stu-id="a456d-186">In these cases, type arguments must be passed along with the identifer.</span></span>

<span data-ttu-id="a456d-187">Eerdere versies van de Q #-compiler hebben een functie waarde toegestaan die moet worden opgegeven als een bewerkings waarde werd verwacht, zolang de functie waarde de juiste invoer-en uitvoer typen had.</span><span class="sxs-lookup"><span data-stu-id="a456d-187">Previous releases of the Q# compiler allowed a function value to be provided where an operation value was expected, as long as the function value had the correct input and output types.</span></span> <span data-ttu-id="a456d-188">Dit was onbedoeld gedrag dat niet is opgegeven in de taal specificatie Q #.</span><span class="sxs-lookup"><span data-stu-id="a456d-188">This was unintended behavior that is not specified in the Q# language specification.</span></span>
<span data-ttu-id="a456d-189">In de 0,3-compiler hebben we dit gedrag verwijderd zodat functie waarden niet meer als bewerkings waarden worden beschouwd.</span><span class="sxs-lookup"><span data-stu-id="a456d-189">In the 0.3 compiler, we have removed this behavior so that function values are no longer considered operation values.</span></span>
<span data-ttu-id="a456d-190">Voor een zo klein mogelijke onderbreking van de bestaande code die voor deze fout is vertrouwd, hebben we de functie ToOperation toegevoegd aan de micro soft. Quantum. Canon-naam ruimte, waarmee een functie expliciet wordt geconverteerd naar een bewerking met dezelfde invoer-en uitvoer typen.</span><span class="sxs-lookup"><span data-stu-id="a456d-190">To minimize the disruption to existing code that relied on this bug, we have added the ToOperation function in the Microsoft.Quantum.Canon namespace that will explicitly convert a function into an operation with the same input and output types.</span></span>

<span data-ttu-id="a456d-191">In het volgende voor beeld ziet u hoe u de `ToOperation` functie kunt gebruiken om een functie `Square` te wijzigen in een bewerking `op`:</span><span class="sxs-lookup"><span data-stu-id="a456d-191">The following example illustrates how to use the `ToOperation` function to change a function `Square` to an operation `op`:</span></span> 
```qsharp
namespace Microsoft.Quantum.Tests {
    open Microsoft.Quantum.Primitive;
    open Microsoft.Quantum.Canon;

    function Square(x : Int) : Int {
        return x * x;
    }

    operation ApplyOp<'T, 'U>(op : ('T => 'U), input : 'T) : 'U {
        return op(input);
    }

    operation ToOperationTest() : Unit {
        let op = ToOperation(Square);
        AssertIntEqual(ApplyOp(op, 3), 9, "ToOperation failed with Square.");
    }
}
```

## <a name="requirements"></a><span data-ttu-id="a456d-192">Vereisten</span><span class="sxs-lookup"><span data-stu-id="a456d-192">Requirements</span></span> 

<span data-ttu-id="a456d-193">De release wordt ondersteund op Visual Studio 2017 versie 15,8 of hoger voor Windows 10 en op Visual Studio code version 1.27.2 of hoger voor Windows 10, macOS en Linux.</span><span class="sxs-lookup"><span data-stu-id="a456d-193">The release is supported on Visual Studio 2017 version 15.8 or later for Windows 10, and on Visual Studio Code version 1.27.2 or later for Windows 10, macOS, and Linux.</span></span>

<span data-ttu-id="a456d-194">De Quantum Development Kit maakt gebruik van de .NET Core SDK (2,0 of hoger).</span><span class="sxs-lookup"><span data-stu-id="a456d-194">The Quantum Development Kit uses the .NET Core SDK (2.0 or later).</span></span>

## <a name="installation"></a><span data-ttu-id="a456d-195">Installatie</span><span class="sxs-lookup"><span data-stu-id="a456d-195">Installation</span></span>  

<span data-ttu-id="a456d-196">Volg de installatie-instructies [hier](../install-guide/index.md).</span><span class="sxs-lookup"><span data-stu-id="a456d-196">Follow the installation instructions [here](../install-guide/index.md).</span></span>

<span data-ttu-id="a456d-197">Mogelijk ziet u waarschuwingen ("gevonden conflicten tussen verschillende versies van System. reflectie. meta gegevens" die niet kunnen worden omgezet. ") als uw huidige versie van .NET Core SDK niet up-to-date is.</span><span class="sxs-lookup"><span data-stu-id="a456d-197">You may see warnings ("Found conflicts between different versions of "System.Reflection.Metadata" that could not be resolved.") if your current version of .NET Core SDK is not up to date.</span></span>  <span data-ttu-id="a456d-198">Als u echter .NET Core SDK (2,0 of hoger) gebruikt, kunnen deze waarschuwingen worden genegeerd.</span><span class="sxs-lookup"><span data-stu-id="a456d-198">However, if you are using .NET Core SDK (2.0 or later), these warnings may be ignored.</span></span>


- <span data-ttu-id="a456d-199">De release wordt weer gegeven samen met een aantal verschillende voor beelden van het gebruik van zowel bestaande functies van de Quantum Development Kit als de nieuwe functies die beschikbaar zijn in deze release.</span><span class="sxs-lookup"><span data-stu-id="a456d-199">The release is provided along with a number of different samples showing how to use both existing features of the Quantum Development Kit, and the new features available with this release.</span></span> <span data-ttu-id="a456d-200">Deze voor beelden kunnen worden gevonden op GitHub in de [micro soft/Quantum-](https://github.com/Microsoft/Quantum) opslag plaats.</span><span class="sxs-lookup"><span data-stu-id="a456d-200">These samples can be can be found on GitHub at the [Microsoft/Quantum](https://github.com/Microsoft/Quantum) repository.</span></span>


## <a name="Migration"></a><span data-ttu-id="a456d-201">Bestaande projecten migreren naar Q # 0,3</span><span class="sxs-lookup"><span data-stu-id="a456d-201">Migrating Existing Projects to Q# 0.3</span></span> 

<span data-ttu-id="a456d-202">Als u bestaande Q #-projecten van versie 0,2 van de Quantum Development Kit hebt, zijn de volgende stappen voor het migreren van die projecten naar de nieuwste versie.</span><span class="sxs-lookup"><span data-stu-id="a456d-202">If you have existing Q# projects from version 0.2 of the Quantum Development Kit, the following are the steps to migrate those projects to the newest version.</span></span> <span data-ttu-id="a456d-203">We bieden ook een [migratie script](#MigrationScript) om u te helpen bij het proces.</span><span class="sxs-lookup"><span data-stu-id="a456d-203">We also provide a [migration script](#MigrationScript) to help you with the process.</span></span>

> [!NOTE]
> <span data-ttu-id="a456d-204">Projecten moeten in volgorde worden bijgewerkt.</span><span class="sxs-lookup"><span data-stu-id="a456d-204">Projects need to be upgraded in order.</span></span> <span data-ttu-id="a456d-205">Als u een oplossing met meerdere projecten hebt, moet u elk project bijwerken in de volg orde waarin ze worden verwezen.</span><span class="sxs-lookup"><span data-stu-id="a456d-205">If you have a solution with multiple projects update each project in the order they are referenced.</span></span>


1. <span data-ttu-id="a456d-206">Voer in een opdrachtregel `dotnet clean` uit om alle bestaande binaire en tussenliggende bestanden te verwijderen.</span><span class="sxs-lookup"><span data-stu-id="a456d-206">From a command line, Run `dotnet clean` to remove all existing binaries and intermediate files.</span></span>
2. <span data-ttu-id="a456d-207">Bewerk het. csproj-bestand in een tekst editor om de versie van alle ' micro soft. Quantum ' `PackageReference` te wijzigen in versie 0.3.1811.2802-preview, bijvoorbeeld:</span><span class="sxs-lookup"><span data-stu-id="a456d-207">In a text editor, edit the .csproj file to change the version of all the "Microsoft.Quantum" `PackageReference` to version 0.3.1811.2802-preview, for example:</span></span>
```xml
    <PackageReference Include="Microsoft.Quantum.Canon" Version="0.3.1811.2802" />
    <PackageReference Include="Microsoft.Quantum.Development.Kit" Version="0.3.1811.2802" />
```
4. <span data-ttu-id="a456d-208">Gebruik op de opdracht regel het opmaak programma dat is geïntegreerd in het opdracht regel programma om alle syntaxis wijzigingen op te lossen door deze opdracht uit te voeren: `dotnet msbuild /t:qsharpformat`</span><span class="sxs-lookup"><span data-stu-id="a456d-208">From the command line, use the formatting tool integrated into the command line compiler to address all syntax changes by running this command: `dotnet msbuild /t:qsharpformat`</span></span>  
    - <span data-ttu-id="a456d-209">Uw bestanden worden in-place gemigreerd.</span><span class="sxs-lookup"><span data-stu-id="a456d-209">Your files will be migrated in-place.</span></span> <span data-ttu-id="a456d-210">Er wordt een back-up van alle originele bestanden gekopieerd naar `obj\qsharp\.backup`</span><span class="sxs-lookup"><span data-stu-id="a456d-210">A backup of all the original files will be copied to `obj\qsharp\.backup`</span></span>
    - <span data-ttu-id="a456d-211">Het hulp programma voor opmaak compileert het project waarbij alle compilatie fouten worden genegeerd en het genereren van opgemaakte Q #-code op basis van compilatie compileren.</span><span class="sxs-lookup"><span data-stu-id="a456d-211">The formatting tool will compile the project ignoring all compilation errors and generate formatted Q# code based on the build compilation.</span></span> 
       <span data-ttu-id="a456d-212">Een niet-herkend symbool (bijvoorbeeld een niet-gedefinieerde naam van een variabele) wordt vervangen door een tekst van een tijdelijke aanduiding die hand matig moet worden vervangen na de opmaak.</span><span class="sxs-lookup"><span data-stu-id="a456d-212">Any unrecognized symbol (e.g. an undefined variable name) will be replaced by a placeholder text that needs to be replaced manually after formatting.</span></span> <span data-ttu-id="a456d-213">In dit geval wordt de opmaak voltooid tijdens het genereren van een waarschuwing voor het betreffende bestand.</span><span class="sxs-lookup"><span data-stu-id="a456d-213">In this case, the formatting succeeds while generating a warning for the affected file.</span></span>     
    - <span data-ttu-id="a456d-214">De opmaak en met name witruimte in het bestand worden gewijzigd in de verzonden code.</span><span class="sxs-lookup"><span data-stu-id="a456d-214">The formatting and in particular any white space in the file will be changed in the emitted code.</span></span> <span data-ttu-id="a456d-215">Opmerkingen blijven behouden.</span><span class="sxs-lookup"><span data-stu-id="a456d-215">Comments will be preserved.</span></span>   
5. <span data-ttu-id="a456d-216">Nadat u dit hebt uitgevoerd, moet u mogelijk nog steeds hand matig semantische wijzigingen aanbrengen in gevallen waarin de semantische interpretatie van de code is gewijzigd.</span><span class="sxs-lookup"><span data-stu-id="a456d-216">After running this, you might still need to manually address semantic changes in cases where the semantic interpretation of the code has changed.</span></span> <span data-ttu-id="a456d-217">Al deze fouten worden door IntelliSense gerapporteerd in Visual Studio of Visual Studio code.</span><span class="sxs-lookup"><span data-stu-id="a456d-217">All these errors will be reported by IntelliSense in Visual Studio or Visual Studio Code.</span></span>
    - <span data-ttu-id="a456d-218">Open de hoofdmap van het project of de container oplossing in Visual Studio 2017 of Visual Studio code.</span><span class="sxs-lookup"><span data-stu-id="a456d-218">Open the root folder of the project or the containing solution in Visual Studio 2017 or Visual Studio Code.</span></span>
    - <span data-ttu-id="a456d-219">Nadat u een .qs-bestand hebt geopend in de editor, zou u de uitvoer van de Q#-taalextensie moeten zien in het uitvoervenster.</span><span class="sxs-lookup"><span data-stu-id="a456d-219">After opening a .qs file in the editor, you should see the output of the Q# language extension in the output window.</span></span>
    - <span data-ttu-id="a456d-220">Nadat het project succesvol is geladen (aangegeven in het uitvoervenster), opent u elk bestand handmatig om de resterende problemen aan te pakken.</span><span class="sxs-lookup"><span data-stu-id="a456d-220">After the project has loaded successfully (indicated in the output window) open each file and manually to address all remaining issues.</span></span>


> [!NOTE]
> * <span data-ttu-id="a456d-221">Voor de 0,3-release ondersteunt de taal server die deel uitmaakt van de Quantum Development Kit geen meerdere werk ruimten.</span><span class="sxs-lookup"><span data-stu-id="a456d-221">For the 0.3 release, the language server included with the Quantum Development Kit does not support multiple workspaces.</span></span>
> * <span data-ttu-id="a456d-222">Als u met een project in Visual Studio Code wilt werken, opent u de hoofdmap met het project zelf en alle projecten waarnaar wordt verwezen.</span><span class="sxs-lookup"><span data-stu-id="a456d-222">In order to work with a project in Visual Studio Code, open the root folder containing the project itself and all referenced projects.</span></span>   
> * <span data-ttu-id="a456d-223">Als u wilt werken met een oplossing in Visual Studio, moeten alle projecten in de oplossing zich in dezelfde map bevinden als de oplossing of in een van de submappen.</span><span class="sxs-lookup"><span data-stu-id="a456d-223">In order to work with a solution in Visual Studio, all projects contained in the solution need to be in the same folder as the solution or in one of its subfolders.</span></span>  
> * <span data-ttu-id="a456d-224">Verwijzingen tussen projecten die zijn gemigreerd naar 0,3 en hoger en projecten die oudere pakket versies gebruiken, worden **niet** ondersteund.</span><span class="sxs-lookup"><span data-stu-id="a456d-224">References between projects migrated to 0.3 and higher and projects using older package versions are **not** supported.</span></span>




## <a name="MigrationScript"></a><span data-ttu-id="a456d-225">Migratie script</span><span class="sxs-lookup"><span data-stu-id="a456d-225">Migration script</span></span> 

<span data-ttu-id="a456d-226">Om project migratie te vergemakkelijken, wordt een Power shell-script aangeboden dat [hier](https://raw.githubusercontent.com/Microsoft/Quantum/release/v0.3.1810/utilities/qdk-migrate.ps1)kan worden gedownload.</span><span class="sxs-lookup"><span data-stu-id="a456d-226">In order to facilitate project migration, a PowerShell script is provided that can be downloaded [here](https://raw.githubusercontent.com/Microsoft/Quantum/release/v0.3.1810/utilities/qdk-migrate.ps1).</span></span>
<span data-ttu-id="a456d-227">Met dit script kunnen projecten worden gemigreerd van versie 0,2 van de Quantum Development Kit om versie 0,3 te gebruiken.</span><span class="sxs-lookup"><span data-stu-id="a456d-227">This script helps migrate projects from version 0.2 of the Quantum Development Kit to use version 0.3.</span></span>

> [!NOTE]
> <span data-ttu-id="a456d-228">Voor het migratie script moet Windows Power shell of Power shell core worden uitgevoerd.</span><span class="sxs-lookup"><span data-stu-id="a456d-228">The migration script requires Windows PowerShell or PowerShell Core to run.</span></span>
> <span data-ttu-id="a456d-229">Windows Power shell wordt vooraf geïnstalleerd met Windows 10.</span><span class="sxs-lookup"><span data-stu-id="a456d-229">Windows PowerShell comes pre-installed with Windows 10.</span></span>
> <span data-ttu-id="a456d-230">Down load Power shell core voor Windows, macOS of Linux op https://github.com/PowerShell/PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a456d-230">Download PowerShell Core for Windows, macOS, or Linux at https://github.com/PowerShell/PowerShell.</span></span>

<span data-ttu-id="a456d-231">Het script voert de volgende vier stappen uit:</span><span class="sxs-lookup"><span data-stu-id="a456d-231">The script executes the following four steps:</span></span>

- <span data-ttu-id="a456d-232">Het project is gebouwd met de vorige versie van de Quantum Development Kit om ervoor te zorgen dat de migratie waarschijnlijk slaagt.</span><span class="sxs-lookup"><span data-stu-id="a456d-232">The project is built with the previous version of the Quantum Development Kit to ensure that migration is likely to succeed.</span></span> <span data-ttu-id="a456d-233">Deze stap kan worden overgeslagen met behulp van de para meter `-Force` naar het script.</span><span class="sxs-lookup"><span data-stu-id="a456d-233">This step can be skipped by using the `-Force` parameter to the script.</span></span>
- <span data-ttu-id="a456d-234">Er worden nieuwe project sjablonen geïnstalleerd.</span><span class="sxs-lookup"><span data-stu-id="a456d-234">New project templates are installed.</span></span>
- <span data-ttu-id="a456d-235">Het project wordt bijgewerkt met de huidige versie van de Quantum Development Kit.</span><span class="sxs-lookup"><span data-stu-id="a456d-235">The project is updated to use the current version of the Quantum Development Kit.</span></span>
- <span data-ttu-id="a456d-236">Het opmaak programma dat wordt meegeleverd met versie 0,3 van de Quantum Development Kit wordt toegepast om de meeste van de hierboven beschreven wijzigingen in het proces automatisch aan te pakken.</span><span class="sxs-lookup"><span data-stu-id="a456d-236">The formatting tool provided with version 0.3 of the Quantum Development Kit is applied to automatically address most of the breaking changes documented above.</span></span>

<span data-ttu-id="a456d-237">Nadat deze vier stappen zijn voltooid, zijn er mogelijk nog andere migratie problemen die hand matig moeten worden afgehandeld.</span><span class="sxs-lookup"><span data-stu-id="a456d-237">After these four steps complete, there may be some remaining migration issues which must be handled manually.</span></span>
<span data-ttu-id="a456d-238">Met de nieuwste versie van de Quantum Development Kit-uitbrei dingen voor Visual Studio 2017 en Visual Studio code kunt u deze problemen eenvoudig vinden.</span><span class="sxs-lookup"><span data-stu-id="a456d-238">Using the latest version of the Quantum Development Kit extensions for Visual Studio 2017 and Visual Studio Code can help find these issues easily.</span></span>

<span data-ttu-id="a456d-239">De documentatie voor het script kan worden verkregen door een van de volgende opdrachten uit te voeren in de map waarin het script zich bevindt:</span><span class="sxs-lookup"><span data-stu-id="a456d-239">The documentation for the script can be obtained by running one of the following commands in the folder where the script is located:</span></span>

```powershell
Get-Help ./qdk-migrate.ps1
Get-Help -Online ./qdk-migrate.ps1
```

> [!NOTE]
> <span data-ttu-id="a456d-240">Standaard wordt voor komen dat scripts worden uitgevoerd als beveiligings maatregel.</span><span class="sxs-lookup"><span data-stu-id="a456d-240">By default, Windows prevents the execution of any scripts as a security measure.</span></span>
> <span data-ttu-id="a456d-241">Als u wilt toestaan dat scripts zoals `qdk-migrate.ps1` worden uitgevoerd in Windows, moet u het uitvoerings beleid mogelijk wijzigen.</span><span class="sxs-lookup"><span data-stu-id="a456d-241">To allow scripts such as `qdk-migrate.ps1` to run on Windows, you may need to change the execution policy.</span></span>
> <span data-ttu-id="a456d-242">U doet dit door de `Set-ExecutionPolicy` opdracht uit te voeren:</span><span class="sxs-lookup"><span data-stu-id="a456d-242">To do so, run the `Set-ExecutionPolicy` command:</span></span>
> ```powershell
> Set-ExecutionPolicy RemoteSigned -Scope Process
> ```
> <span data-ttu-id="a456d-243">Het uitvoerings beleid wordt dan teruggezet wanneer u Power shell afsluit.</span><span class="sxs-lookup"><span data-stu-id="a456d-243">The execution policy will then be reverted when you exit PowerShell.</span></span>
> <span data-ttu-id="a456d-244">Als u het uitvoerings beleid wilt opslaan, gebruikt u een andere waarde voor `-Scope`:</span><span class="sxs-lookup"><span data-stu-id="a456d-244">If you would like to save the execution policy, use a different value for `-Scope`:</span></span>
> ```powershell
> Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```

